# Copy to .env locally if you prefer; the apps read process environment variables.
# Do not commit real tokens.

API_ENDPOINT=https://DATABASE_ID-REGION.apps.astra.datastax.com
APPLICATION_TOKEN=AstraCS:...
KEYSPACE_NAME=default_keyspace
